import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Waypoint")
            .font(.largeTitle)
            .fontWeight(.semibold)
    }
}

#Preview {
    ContentView()
}
